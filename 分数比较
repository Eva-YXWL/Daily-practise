#include<stdio.h>
main()
{
	int a,b,c,d;
	printf("Input a/b, c/d:");
	scanf("%d/%d,%d/%d",&a,&b,&c,&d);
	if(a*d>b*c)
	{
		printf("%d/%d>%d/%d\n",a,b,c,d);
	}
	if(a*d<b*c)
	{
		printf("%d/%d<%d/%d\n",a,b,c,d);
	}
	if(a*d==b*c)
	{
		printf("%d/%d=%d/%d\n",a,b,c,d);
	}
}
