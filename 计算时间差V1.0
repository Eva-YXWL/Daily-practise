#include<stdio.h>
#include<math.h>
main()
{
int a,b,c,d,x,y; 
printf("Input time one(hour, minute):");
scanf("%d,%d",&a,&b);
printf("Input time two(hour, minute):");
scanf("%d,%d",&c,&d);
x=((60*a+b)-(60*c+d))/60;
y=((60*a+b)-(60*c+d))%60;
x=abs(x);
y=abs(y); 
printf("%d hour %d minute\n",x,y);
}
